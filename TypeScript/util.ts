export function add(x: number, y: number): number {
    return x + y;
}

export function sub(x: number, y: number): number {
    return x - y;
}
 
function test() {
  return "test";
}

// export const var = 1;

export default test;